@using Microsoft.AspNetCore.Mvc.ModelBinding.Binders
@model Bookish.Models.ListOfBooks

@{
    ViewBag.Title = "Library's Catalogue Display";
}
<meta charset="UTF-8"></meta>
<h2>Library's Catalogue Display &#128512</h2>
<h4>(In order of most searched)</h4>

<div class="row">

    <table style="width:100%">
        <tr>
            <th>ISBN</th>
            <th>Title</th>
            <th>Author</th>
            <th>Number of total copies</th>
            <th>Number of available copies</th>
            <th>Number of searches</th>
        </tr>
        
        @{
            if (Model.AllBooks.Count >= 1)
            {
                foreach (var bookCopy in Model.AllBooks)
                {
       
                    <tr>
                        <td>@bookCopy.Id</td>
                        <td>@bookCopy.Title</td>
                        <td>@bookCopy.Author</td>
                        <td>@bookCopy.NumOfCopies</td>
                        <td>@bookCopy.NumOfAvailableCopies</td>
                        <td>@bookCopy.Searches</td>
                        <td><button class="editbtn">Edit</button></td>
                        <td><li class="nav-item">
                            <a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Index">Edit</a>
                        </li></td>
                    </tr>
                }
            }
        }

    </table>
    <p></p>
    
  
</div>